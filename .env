# 所有模式共用的預設值。.env.[mode] 會覆蓋這裡。
#
# 這個變數一定要有值，理由見 src/env.ts：Vite 只會把「有定義」的 VITE_* 換成
# 字面值，沒定義就退回執行期判斷，內部簡報就不會被 tree-shake 掉。
#
# 預設刻意是 public：萬一 .env 沒被讀到，寧可對外站少幾份簡報，
# 也不要把內部簡報漏出去。
VITE_DECK_MODE=public
